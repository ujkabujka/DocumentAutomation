<Window x:Class="DocumentAutomation.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:navigation="clr-namespace:BaseFramework.Wpf.Controls.Navigation;assembly=BaseFramework.Wpf"
        mc:Ignorable="d"
        Title="Document Automation"
        Width="1500"
        Height="960"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource SurfaceBrush}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="#1A1A1F"
                CornerRadius="10"
                Padding="16"
                Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="Document Automation Platform"
                               FontSize="28"
                               FontWeight="Bold" />
                    <TextBlock Text="Role-based engineering document automation powered by the evolved BaseFramework inspector."
                               Opacity="0.72"
                               Margin="0,4,0,0" />
                </StackPanel>

                <StackPanel Grid.Column="1"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center">
                    <TextBlock x:Name="CurrentUserText"
                               FontWeight="SemiBold"
                               HorizontalAlignment="Right" />
                    <TextBlock x:Name="CurrentRoleText"
                               Opacity="0.76"
                               HorizontalAlignment="Right" />
                    <TextBlock x:Name="ConnectionStatusText"
                               Opacity="0.76"
                               HorizontalAlignment="Right" />
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1"
                x:Name="DemoModeBanner"
                Background="#50391F"
                BorderBrush="#FFB36C"
                BorderThickness="1"
                CornerRadius="8"
                Padding="12"
                Margin="0,0,0,12"
                Visibility="Collapsed">
            <TextBlock x:Name="DemoModeText"
                       TextWrapping="Wrap" />
        </Border>

        <navigation:NavigationHostControl Grid.Row="2"
                                          x:Name="NavigationHost" />
    </Grid>
</Window>
